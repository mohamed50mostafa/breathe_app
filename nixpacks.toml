[phases.setup]
cmds = [
    "apt-get update",
    "apt-get install -y libsqlite3-dev libpq5",
]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
]

[phases.build]
cmds = [
    "python manage.py collectstatic --noinput",
]

[start]
cmd = "python manage.py migrate && gunicorn breathe_app.wsgi:application --bind 0.0.0.0:$PORT"